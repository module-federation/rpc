{
  "name": "mf-typesafe-rpc",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.19.33",
    "expect-type": "^0.19.0",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
